# Core dependencies for DINO benchmark
numpy>=1.19.0
psutil>=5.8.0
matplotlib>=3.3.0

# PyTorch (optional, will fallback to dummy mode if not available)
torch>=1.8.0; python_version >= "3.7"
torchvision>=0.9.0; python_version >= "3.7"

# Image processing (optional)
Pillow>=8.0.0

# Additional utilities
tqdm>=4.60.0